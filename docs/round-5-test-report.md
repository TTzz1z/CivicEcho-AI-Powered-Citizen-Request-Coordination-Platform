# 第五轮测试报告

执行日期：2026-07-14（Asia/Shanghai）。所有数字均来自本轮实际命令输出。

## 已完成验证

- 前端生产构建：通过；TypeScript 与 Vite 构建成功，并使用路由懒加载分包。
- 前端组件测试：7 个文件、8 个用例，覆盖登录校验、角色守卫、状态标签、URL 筛选、权限操作、409 语义、错误态和聊天渲染。
- Playwright Chromium：17 个用例通过，覆盖四角色入口、管理员页面、市民越权阻断、真实 Rasa 多轮创建诉求、Backend/Rasa 故障降级、停用账号、409 并发冲突，以及同一张聊天工单在页面完成创建、受理、派发、认领、处理记录、解决、查看进度和办结的完整状态闭环。
- Backend：Compose 网络内 `18 passed`；新增用例验证登录市民可访问本人稳定 Rasa sender 创建的工单，其他市民仍返回 403。
- Action Server：公开诉求动作 18 个用例、Backend 网关 3 个用例通过。
- Rasa：数据校验通过；Core 回归 24/24 stories、113/113 actions 全部通过。
- 真实接口：管理员 JWT 登录成功；看板返回 4 个指标、最近工单与分布；审计分页返回真实记录。
- Docker：六个服务完成镜像构建和启动；HTTP 健康端点均实际访问成功。

## 说明

本机默认 Python 环境没有安装 pytest，改在 Compose Backend 容器安装开发依赖后执行，18 项全部通过。浏览器回归发现并修复两处真实联调问题：聊天页仍按旧 12 位规则截断当前 16 位工单号；Rasa 服务身份创建的 `web-user-{id}` 工单未纳入登录市民的数据范围。部门端还补齐了开始处理时认领承办人、`mine=true` 服务端筛选和携带 version 的独立处理记录。最终同一张聊天工单的 8 步页面闭环及全量 17 项 E2E 均通过。

## 尚未声称完成

本报告不把尚未执行的生产压力测试、真实政务平台联调或 Firefox/WebKit 浏览器矩阵写为通过。当前闭环测试会写入本地 PostgreSQL；生产验收应改用一次性或可重置的隔离测试数据库。
